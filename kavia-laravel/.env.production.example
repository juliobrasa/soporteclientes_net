# Configuración para hosting soporteclientes.net/web
# Renombrar a .env en el servidor

APP_NAME="Kavia Admin Panel"
APP_ENV=production
APP_KEY=base64:GENERAR_NUEVA_KEY_EN_SERVIDOR
APP_DEBUG=false
APP_URL=https://soporteclientes.net/web

APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_ES

APP_MAINTENANCE_DRIVER=file

LOG_CHANNEL=single
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# Database - Usar las mismas credenciales del sistema actual
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=soporteia_bookingkavia
DB_USERNAME=soporteia_admin
DB_PASSWORD="QCF8RhS*}.Oj0u(v"

# Sessions y Cache
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false

CACHE_STORE=database
QUEUE_CONNECTION=database

# Security
BCRYPT_ROUNDS=12

# Mail
MAIL_MAILER=log
MAIL_FROM_ADDRESS="admin@soporteclientes.net"
MAIL_FROM_NAME="${APP_NAME}"

# Optimización para producción
FILESYSTEM_DISK=local
BROADCAST_CONNECTION=log